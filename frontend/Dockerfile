# TODO: 프론트엔드 스택 확정 후 이 Dockerfile을 교체하세요.
# React  → node:22-alpine 빌드 + nginx:alpine 서빙
# Next.js → node:22-alpine 빌드 + node:22-alpine 실행
# Vue    → node:22-alpine 빌드 + nginx:alpine 서빙

FROM nginx:alpine
COPY index.html /usr/share/nginx/html/index.html
EXPOSE 80
